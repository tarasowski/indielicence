# CLAUDE.md

Read and follow `AGENTS.md` in this directory — it contains the operating
instructions and the non-negotiable safety rules for this repository (most
importantly: never read, copy, or commit `*.private` files, never touch
`*.state`, and never run destructive or revocation commands without the
user's explicit request). When setting up IndieLicense in another application,
also follow `AI_INTEGRATION.md` as the canonical integration procedure.
